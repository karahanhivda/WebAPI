@using Galeri.Data
@model Araba

<div class="alert alert-info">
    Silmek istediğinize emin misiniz?
</div>
<div class="alert alert-secondary">
    Marka: @Model.Marka Renk: @Model.Renk
</div>
<form asp-action="Delete" method="post">
    <input type="submit" class="btn btn-primary" value="EVET"/>
    <a asp-action="Index" class="btn btn-danger">HAYIR</a>
</form>